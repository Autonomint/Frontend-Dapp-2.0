# Commands
- Build: `npm run build` (run before every commit)
- Dev: `npm run dev`
- Lint: `npm run lint`
- Type check: `npx tsc --noEmit` (use instead of build during development)

# Commit Process
Always run `npm run build` successfully before committing and pushing.
